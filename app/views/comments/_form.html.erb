<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for :comment, url: review_comments_path do |f| %> 
        <%= f.label :content%>
        <%= f.text_area :content%>
</div>

    <%= f.submit "Post"%>
    <% end %>